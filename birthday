<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Magical Birthday Surprise</title>
  <style>
    :root{--bg:#0a0f1c;--accent:#ff77e9;--glow:#7df9ff}
    html,body{height:100%;margin:0;font-family:'Poppins',system-ui,sans-serif;color:#fff;background:radial-gradient(circle at top,#0a0f1c 0%,#000 100%);display:grid;place-items:center;overflow:hidden}

    .stage{text-align:center;position:relative;z-index:2}
    h1{font-size:1.5rem;margin:0;color:var(--glow);text-shadow:0 0 10px var(--glow)}
    .hint{color:rgba(255,255,255,.6);margin-top:10px;font-size:.9rem}

    /* Gift box */
    .gift-wrap{width:240px;height:240px;margin:40px auto;position:relative;cursor:pointer;transition:transform .3s}
    .gift-wrap:hover{transform:scale(1.05)}
    .box{width:100%;height:70%;background:#e63946;border-radius:10px;position:absolute;bottom:0;left:0;box-shadow:0 18px 40px rgba(0,0,0,.6)}
    .box-top{width:100%;height:45%;background:#d62839;border-radius:10px;position:absolute;top:0;left:0;box-shadow:0 6px 12px rgba(0,0,0,.5);transition:.6s}
    .ribbon,.ribbon.cross{position:absolute;background:linear-gradient(180deg,#ffe66d,#ff9f43);z-index:2}
    .ribbon{left:50%;top:0;transform:translateX(-50%);width:60px;height:100%;border-radius:6px}
    .ribbon.cross{top:50%;left:0;transform:translateY(-50%);width:100%;height:30px;border-radius:6px}
    .bow{position:absolute;top:-20px;left:50%;transform:translateX(-50%);width:80px;height:44px;background:linear-gradient(90deg,#ffe66d,#ff9f43);border-radius:14px 14px 8px 8px;box-shadow:0 0 10px rgba(255,255,200,.6)}
    .bow::before,.bow::after{content:'';position:absolute;width:28px;height:38px;background:linear-gradient(90deg,#ffe66d,#ff9f43);border-radius:6px}
    .bow::before{left:-14px;transform:rotate(-25deg)}
    .bow::after{right:-14px;transform:rotate(25deg)}

    /* Overlay */
    .overlay{position:fixed;inset:0;background:rgba(0,0,20,.7);backdrop-filter:blur(6px);display:grid;place-items:center;z-index:30;opacity:0;pointer-events:none;transition:.4s}
    .overlay.show{opacity:1;pointer-events:auto}
    .card{background:#111827;padding:28px;border-radius:16px;min-width:300px;box-shadow:0 12px 50px rgba(255,255,255,.2);text-align:center;animation:pop .5s ease}
    @keyframes pop{0%{transform:scale(.6);opacity:0}100%{transform:scale(1);opacity:1}}
    .card h2{margin:0 0 14px;font-size:1.3rem;color:var(--accent);text-shadow:0 0 10px var(--accent)}
    .card input{width:100%;padding:12px;border-radius:8px;border:none;outline:none;background:rgba(255,255,255,.1);color:#fff;margin-bottom:12px;font-size:1rem}
    .card button{padding:10px 16px;border-radius:10px;border:none;background:linear-gradient(90deg,#7df9ff,#ff77e9);color:#000;cursor:pointer;font-weight:600;transition:.3s}
    .card button:hover{transform:scale(1.05)}

    /* Celebration */
    .celebrate{position:fixed;inset:0;pointer-events:none;display:flex;align-items:center;justify-content:center;flex-direction:column;z-index:20}
    .message{font-size:3rem;font-weight:800;text-align:center;text-shadow:0 0 20px var(--accent),0 0 40px var(--accent);opacity:0;transform:translateY(-30px);transition:.6s}
    .message.show{opacity:1;transform:translateY(0)}

    /* Balloons */
    .balloon{position:absolute;width:90px;height:120px;border-radius:60% 60% 50% 50%;display:flex;align-items:flex-end;justify-content:center;padding-bottom:12px;box-shadow:0 10px 20px rgba(0,0,0,.25)}
    .balloon::after{content:'';width:2px;height:70px;background:#aaa;display:block}
    .b1{left:10%;bottom:-15%;animation:float1 6s infinite ease-in-out}
    .b2{right:10%;bottom:-20%;animation:float2 7s infinite ease-in-out}
    .b3{left:40%;bottom:-10%;animation:float3 8s infinite ease-in-out}
    @keyframes float1{0%{transform:translateY(0)}50%{transform:translateY(-45vh)}100%{transform:translateY(0)}}
    @keyframes float2{0%{transform:translateY(0)}50%{transform:translateY(-50vh)}100%{transform:translateY(0)}}
    @keyframes float3{0%{transform:translateY(0)}50%{transform:translateY(-55vh)}100%{transform:translateY(0)}}

    /* Fireworks */
    .firework{position:absolute;width:6px;height:6px;border-radius:50%;background:var(--accent);animation:explode 1.2s forwards;}
    @keyframes explode{0%{transform:scale(0);opacity:1}80%{transform:scale(1.5);opacity:1}100%{transform:scale(0);opacity:0}}

    /* Stars background */
    .stars{position:absolute;inset:0;background:transparent;overflow:hidden;z-index:0}
    .star{position:absolute;width:2px;height:2px;background:white;border-radius:50%;animation:twinkle 3s infinite alternate}
    @keyframes twinkle{from{opacity:.2}to{opacity:1}}

  </style>
</head>
<body>
  <div class="stage">
    <h1>✨ Magical Birthday Surprise ✨</h1>
    <div class="hint">Click the gift box to reveal your surprise</div>
    <div class="gift-wrap" id="gift">
      <div class="box-top" id="lid"></div>
      <div class="box" id="base"></div>
      <div class="ribbon"></div>
      <div class="ribbon cross"></div>
      <div class="bow"></div>
    </div>
  </div>

  <div class="overlay" id="overlay">
    <div class="card">
      <h2>Enter Your Name 🎂</h2>
      <input id="nameInput" placeholder="Type name..." />
      <button id="sendBtn">Show Surprise</button>
    </div>
  </div>

  <div class="celebrate" id="celebrate">
    <div class="message" id="message">Happy Birthday!</div>
  </div>

  <div class="stars" id="stars"></div>

  <script>
    const gift=document.getElementById('gift');
    const lid=document.getElementById('lid');
    const overlay=document.getElementById('overlay');
    const nameInput=document.getElementById('nameInput');
    const sendBtn=document.getElementById('sendBtn');
    const celebrate=document.getElementById('celebrate');
    const message=document.getElementById('message');
    const stars=document.getElementById('stars');

    // stars background
    for(let i=0;i<120;i++){
      const s=document.createElement('div');
      s.className='star';
      s.style.top=Math.random()*100+'%';
      s.style.left=Math.random()*100+'%';
      s.style.animationDelay=(Math.random()*3)+'s';
      stars.appendChild(s);
    }

    gift.addEventListener('click',()=>{
      lid.style.transform='translateY(-60%) rotateX(40deg)';
      setTimeout(()=>overlay.classList.add('show'),600);
    });

    sendBtn.addEventListener('click',()=>{
      overlay.classList.remove('show');
      const name=nameInput.value.trim()||'Friend';
      showCelebration(name);
    });

    function showCelebration(name){
      message.textContent=`🎉 Happy Birthday, ${name}! 🎉`;
      setTimeout(()=>message.classList.add('show'),200);

      // Balloons
      const colors=['linear-gradient(180deg,#ff9a9e,#fecfef)','linear-gradient(180deg,#a1c4fd,#c2e9fb)','linear-gradient(180deg,#fbc2eb,#a6c1ee)'];
      for(let i=0;i<6;i++){
        const b=document.createElement('div');
        b.className='balloon';
        b.style.left=(5+Math.random()*90)+'%';
        b.style.bottom='-25%';
        b.style.background=colors[i%colors.length];
        b.style.animationDelay=(Math.random()*2)+'s';
        celebrate.appendChild(b);
      }

      // Fireworks
      for(let i=0;i<25;i++){
        const f=document.createElement('div');
        f.className='firework';
        f.style.left=(Math.random()*100)+'%';
        f.style.top=(Math.random()*50)+'%';
        f.style.background=['#ff77e9','#7df9ff','#ffe66d'][i%3];
        f.style.animationDelay=(Math.random()*1.2)+'s';
        celebrate.appendChild(f);
        setTimeout(()=>f.remove(),2000);
      }
    }
  </script>
<!-- Code injected by live-server -->
<script>
	// <![CDATA[  <-- For SVG support
	if ('WebSocket' in window) {
		(function () {
			function refreshCSS() {
				var sheets = [].slice.call(document.getElementsByTagName("link"));
				var head = document.getElementsByTagName("head")[0];
				for (var i = 0; i < sheets.length; ++i) {
					var elem = sheets[i];
					var parent = elem.parentElement || head;
					parent.removeChild(elem);
					var rel = elem.rel;
					if (elem.href && typeof rel != "string" || rel.length == 0 || rel.toLowerCase() == "stylesheet") {
						var url = elem.href.replace(/(&|\?)_cacheOverride=\d+/, '');
						elem.href = url + (url.indexOf('?') >= 0 ? '&' : '?') + '_cacheOverride=' + (new Date().valueOf());
					}
					parent.appendChild(elem);
				}
			}
			var protocol = window.location.protocol === 'http:' ? 'ws://' : 'wss://';
			var address = protocol + window.location.host + window.location.pathname + '/ws';
			var socket = new WebSocket(address);
			socket.onmessage = function (msg) {
				if (msg.data == 'reload') window.location.reload();
				else if (msg.data == 'refreshcss') refreshCSS();
			};
			if (sessionStorage && !sessionStorage.getItem('IsThisFirstTime_Log_From_LiveServer')) {
				console.log('Live reload enabled.');
				sessionStorage.setItem('IsThisFirstTime_Log_From_LiveServer', true);
			}
		})();
	}
	else {
		console.error('Upgrade your browser. This Browser is NOT supported WebSocket for Live-Reloading.');
	}
	// ]]>
</script>
</body>
</html>